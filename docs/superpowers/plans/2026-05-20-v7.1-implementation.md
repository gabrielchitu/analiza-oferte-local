# v7.1 — Matching Ierarhic cu Catalog Normativ

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Înlocuiește fuzzy matching greșit (`$6720287`→CK26A) cu matching ierarhic exact bazat pe catalog normativ din referință, plus tratament defensiv pentru articole fără deviz sau fără parent.

**Architecture:** `build_ref_catalog()` extrage maparea `{$cod → normative_cod}` din referință. Layer 0 în `match_global` procesează subarticolele (`$`+parent) cu cheie `(deviz, catalog_parent, $cod)` înaintea Layer 1-4. Articolele fără deviz sunt colectate și returnate separat. Raportul DOCX afișează cazurile speciale la final.

**Tech Stack:** Python 3.11, python-docx; fără dependențe noi.

**Branch:** `feature/7.1` (creat din `feature/7.0`)

**Spec:** `docs/superpowers/specs/2026-05-20-v7.1-matching-ierarhic-design.md`

**Baseline metrici (feature/7.0):** OF1 matched=1041/lipsa=3/extra=14, OF2 matched=1060/lipsa=90/extra=22

---

## Fișiere modificate

| Fișier | Tip | Modificare |
|--------|-----|-----------|
| `AgentComparator_local.py` | Modify | `build_ref_catalog()`, Layer 0, Caz A/B, return 4-tuple |
| `local_run.py` | Modify | Unpack 4-tuple, pasează `articole_fara_deviz` la raport |
| `shared/report_builder.py` | Modify | Acceptă `articole_fara_deviz`, adaugă în output |
| `shared/report_word.py` | Modify | Secțiune "Articole nelocalizate", rând Caz A cu ⚠ |
| `tests/test_matching.py` | Modify | Teste Layer 0 + Caz A/B |
| `tests/test_report_builder.py` | Modify | Teste articole_fara_deviz în raport |

---

## Task 1: `build_ref_catalog` și separare articole fără deviz

**Files:**
- Modify: `AgentComparator_local.py:206-244`
- Modify: `tests/test_matching.py`

- [ ] **Step 1.1: Scrie testele**

```python
# tests/test_matching.py — adaugă

def test_build_ref_catalog_extracts_dollar_parents():
    from AgentComparator_local import build_ref_catalog
    ref = [
        {'cod': 'CK25A', 'deviz': '4.1-03', 'is_component': False, 'display_parent_cod': None},
        {'cod': '$6720289', 'deviz': '4.1-03', 'is_component': False, 'display_parent_cod': 'CK25A'},
        {'cod': '$6720301', 'deviz': '4.1-03', 'is_component': False, 'display_parent_cod': 'CK25A'},
        {'cod': 'RPIF09C', 'deviz': '4.1-10', 'is_component': False, 'display_parent_cod': None},
        {'cod': '$2608118', 'deviz': '4.1-10', 'is_component': False, 'display_parent_cod': 'RPIF09C'},
    ]
    catalog = build_ref_catalog(ref)
    assert catalog['$6720289'] == 'CK25A'
    assert catalog['$6720301'] == 'CK25A'
    assert catalog['$2608118'] == 'RPIF09C'
    assert 'CK25A' not in catalog  # non-$ not in catalog
    assert 'RPIF09C' not in catalog

def test_build_ref_catalog_excludes_no_parent():
    from AgentComparator_local import build_ref_catalog
    ref = [
        {'cod': '$9999', 'deviz': '4.1-03', 'is_component': False, 'display_parent_cod': None},
    ]
    catalog = build_ref_catalog(ref)
    assert '$9999' not in catalog

def test_match_global_separates_articles_without_deviz():
    """Articles without deviz returned as 4th tuple element, not matched."""
    from AgentComparator_local import match_global
    ref = [
        {'cod': 'TF24A', 'deviz': '', 'denumire': 'fara deviz', 'um': 'mp',
         'cantitate': 10.0, 'is_component': False, 'display_parent_cod': None,
         **{f: 0 for f in ['pret_material','val_material','pret_manopera','val_manopera',
                           'pret_utilaj','val_utilaj','pret_transport','val_transport']}},
    ]
    ncs, matches, _, fara_deviz = match_global(ref, [], None, None)
    assert len(fara_deviz) == 1
    assert fara_deviz[0][0] == 'ref'
    assert fara_deviz[0][1]['cod'] == 'TF24A'
    assert len(matches) == 0  # not matched
```

- [ ] **Step 1.2: Rulează testele — FAIL**

```bash
cd /Users/gabriel.chitu/Proiecte/analiza-oferte-EP/analiza-oferte-local && .venv/bin/python -m pytest tests/test_matching.py::test_build_ref_catalog_extracts_dollar_parents tests/test_matching.py::test_match_global_separates_articles_without_deviz -v
```
Așteptat: FAIL — `build_ref_catalog` nu există.

- [ ] **Step 1.3: Implementează `build_ref_catalog` în `AgentComparator_local.py`**

Adaugă DUPĂ `_deduplicate_neconformitati` (linia ~190) și ÎNAINTE de `match_global`:

```python
def build_ref_catalog(ref_articole: list) -> dict:
    """Catalog canonical: $cod → normative_cod, extras exclusiv din referință.

    Sursa de adevăr pentru parent mapping în Layer 0.
    Oferta nu contribuie — elimină inconsistențele de extracție PDF.
    """
    catalog = {}
    for art in ref_articole:
        cod = art.get('cod', '')
        parent = art.get('display_parent_cod')
        if cod.startswith('$') and parent:
            catalog[cod] = parent
    return catalog
```

- [ ] **Step 1.4: Modifică `match_global` — separă articolele fără deviz**

În `match_global`, ÎNAINTE de orice procesare (prima linie din funcție după docstring), adaugă:

```python
    # Caz B: orice articol fără deviz → exclus matching, returnat separat
    articole_fara_deviz: list = []
    ref_articole_valid = []
    for a in ref_articole:
        if not a.get('deviz'):
            articole_fara_deviz.append(('ref', a))
        else:
            ref_articole_valid.append(a)
    ref_articole = ref_articole_valid

    oferta_articole_valid = []
    for a in oferta_articole:
        if not a.get('deviz'):
            articole_fara_deviz.append(('oferta', a))
        else:
            oferta_articole_valid.append(a)
    oferta_articole = oferta_articole_valid
```

- [ ] **Step 1.5: Modifică return-ul `match_global`**

Localizare: `AgentComparator_local.py:952`. Schimbă:

```python
    return neconformitati, matches, matched_ref_keys
```

Cu:

```python
    return neconformitati, matches, matched_ref_keys, articole_fara_deviz
```

- [ ] **Step 1.6: Fix `local_run.py` — unpack 4-tuple**

Localizare: `local_run.py:639`:

```python
    neconformitati, matches, matched_ref_keys, articole_fara_deviz = match_global(
        ref_articles, oferta_norm, client, model, include_prices=include_prices
    )
```

- [ ] **Step 1.7: Rulează testele**

```bash
.venv/bin/python -m pytest tests/test_matching.py -v -q 2>&1 | tail -10
```
Așteptat: toate PASS.

- [ ] **Step 1.8: Verificare rapidă pipeline**

```bash
.venv/bin/python -c "from AgentComparator_local import match_global, build_ref_catalog; print('OK')"
```

- [ ] **Step 1.9: Commit**

```bash
git add AgentComparator_local.py local_run.py tests/test_matching.py
git commit -m "feat(comparator): build_ref_catalog, separate articles without deviz (Case B)"
```

---

## Task 2: Layer 0 — matching ierarhic cu ref_catalog

**Files:**
- Modify: `AgentComparator_local.py:220-260` (dedup section)
- Modify: `tests/test_matching.py`

- [ ] **Step 2.1: Scrie testele Layer 0**

```python
# tests/test_matching.py — adaugă

def test_layer0_matches_subarticle_by_hierarchy():
    """$6720289 (ref, parent=CK25A) matches offer $6720289 (same cod, same deviz+parent)."""
    from AgentComparator_local import match_global
    base = {'um': 'mp', 'cantitate': 16.19, 'denumire': 'usa tamplarie pvc',
            'deviz_denumire': 'Arhitectura', 'is_component': False,
            **{f: 0 for f in ['pret_material','val_material','pret_manopera','val_manopera',
                               'pret_utilaj','val_utilaj','pret_transport','val_transport']}}
    ref = [
        {**base, 'cod': 'CK25A', 'deviz': '4.1-03', 'display_parent_cod': None},
        {**base, 'cod': '$6720289', 'deviz': '4.1-03', 'display_parent_cod': 'CK25A'},
    ]
    oferta = [
        {**base, 'cod': 'CK25A', 'deviz': '4.1-03', 'display_parent_cod': None},
        {**base, 'cod': '$6720289', 'deviz': '4.1-03', 'display_parent_cod': 'CK25A'},
    ]
    ncs, matches, _, _ = match_global(ref, oferta, None, None)
    assert any(m.get('ref_cod') == '$6720289' for m in matches)
    lipsa = [n for n in ncs if n.get('tip') == 'ARTICOL_LIPSA']
    assert not any(n.get('ref_cod') == '$6720289' for n in lipsa)

def test_layer0_lipsa_when_subarticle_missing_in_offer():
    """$6720289 (ref) not in offer → ARTICOL_LIPSA."""
    from AgentComparator_local import match_global
    base = {'um': 'mp', 'cantitate': 16.19, 'denumire': 'usa tamplarie pvc',
            'deviz_denumire': 'Arhitectura', 'is_component': False,
            **{f: 0 for f in ['pret_material','val_material','pret_manopera','val_manopera',
                               'pret_utilaj','val_utilaj','pret_transport','val_transport']}}
    ref = [
        {**base, 'cod': 'CK25A', 'deviz': '4.1-03', 'display_parent_cod': None},
        {**base, 'cod': '$6720289', 'deviz': '4.1-03', 'display_parent_cod': 'CK25A'},
    ]
    oferta = [
        {**base, 'cod': 'CK25A', 'deviz': '4.1-03', 'display_parent_cod': None},
        # $6720287 instead of $6720289 — different code, same parent
        {**base, 'cod': '$6720287', 'deviz': '4.1-03', 'display_parent_cod': 'CK25A'},
    ]
    ncs, matches, _, _ = match_global(ref, oferta, None, None)
    lipsa = [n for n in ncs if n.get('tip') == 'ARTICOL_LIPSA']
    extra = [n for n in ncs if n.get('tip') == 'ARTICOL_EXTRA']
    assert any(n.get('ref_cod') == '$6720289' for n in lipsa), "Expected LIPSA for $6720289"
    assert any(n.get('oferta_cod') == '$6720287' for n in extra), "Expected EXTRA for $6720287"

def test_layer0_extra_for_dollar_not_in_catalog():
    """$6720287 not in ref_catalog → ARTICOL_EXTRA directly."""
    from AgentComparator_local import match_global
    base = {'um': 'mp', 'cantitate': 6.24, 'denumire': 'usa profile pvc',
            'deviz_denumire': 'Arhitectura', 'is_component': False,
            **{f: 0 for f in ['pret_material','val_material','pret_manopera','val_manopera',
                               'pret_utilaj','val_utilaj','pret_transport','val_transport']}}
    ref = [
        {**base, 'cod': 'CK25A', 'deviz': '4.1-03', 'display_parent_cod': None},
    ]
    oferta = [
        {**base, 'cod': '$6720287', 'deviz': '4.1-03', 'display_parent_cod': 'CK25A'},
    ]
    ncs, matches, _, _ = match_global(ref, oferta, None, None)
    extra = [n for n in ncs if n.get('tip') == 'ARTICOL_EXTRA']
    assert any(n.get('oferta_cod') == '$6720287' for n in extra)
```

- [ ] **Step 2.2: Rulează testele — FAIL**

```bash
.venv/bin/python -m pytest tests/test_matching.py::test_layer0_lipsa_when_subarticle_missing_in_offer -v
```
Așteptat: FAIL — `$6720289` nu e LIPSA (e fuzzy-matched).

- [ ] **Step 2.3: Implementează Layer 0 în `match_global`**

Localizare: în `match_global`, DUPĂ separarea articolelor fără deviz (Task 1), ÎNAINTE de `ref_dedup` construction. Înlocuiește blocul `ref_seen/ref_dedup/oferta_seen/oferta_dedup` cu:

```python
    # Build canonical parent catalog from reference
    ref_catalog = build_ref_catalog(ref_articole)

    def _is_breviar_artifact(a: dict) -> bool:
        cant = a.get("cantitate") or 0
        um = (a.get("um") or "").strip()
        return cant == 0 and (not um or um == um.upper())

    # Separate subarticles ($ with parent in catalog) from main articles
    # Caz A: $ with deviz but no parent → excluded from matching, collected separately
    ref_caz_a: list = []  # $ no parent, has deviz → display with ⚠ in report

    ref_seen = {}
    ref_dedup = []       # main articles for Layer 1-4
    ref_subarticole = [] # $ with parent in catalog → Layer 0
    for a in ref_articole:
        if not a.get("cod") or _is_breviar_artifact(a):
            continue
        cod = a.get("cod", "")
        if cod.startswith("$") and not a.get("is_component"):
            if cod in ref_catalog:
                ref_subarticole.append(a)
            else:
                ref_caz_a.append(a)  # Caz A: no parent in catalog
        elif not a.get("is_component"):
            key = (a.get("deviz"), cod, a.get("um"), a.get("cantitate"))
            if key not in ref_seen:
                ref_dedup.append(a)
                ref_seen[key] = True

    oferta_caz_a: list = []  # offer $ no parent in catalog and no display_parent_cod

    oferta_seen = {}
    oferta_dedup = []           # main articles for Layer 1-4
    oferta_sub_catalog = []     # $ in ref_catalog → Layer 0
    oferta_sub_new = []         # $ NOT in ref_catalog but has display_parent_cod → EXTRA
    oferta_sub_no_parent = []   # $ NOT in ref_catalog, no display_parent_cod → Caz A
    for a in oferta_articole:
        if not a.get("cod") or _is_breviar_artifact(a):
            continue
        cod = a.get("cod", "")
        if cod.startswith("$"):
            if cod in ref_catalog:
                oferta_sub_catalog.append(a)
            elif a.get("display_parent_cod"):
                oferta_sub_new.append(a)  # new $ with known parent → EXTRA
            else:
                oferta_sub_no_parent.append(a)  # Caz A in offer
        else:
            key = (a.get("deviz"), cod, a.get("um"), a.get("cantitate"))
            if key not in oferta_seen:
                oferta_dedup.append(a)
                oferta_seen[key] = True

    # ── Layer 0: Matching ierarhic (deviz + catalog_parent + $cod) ───────────────
    # Index oferta subarticole folosind parent DIN CATALOG (nu din ofertă)
    oferta_hier: dict = {}  # (deviz, catalog_parent, $cod) → oferta_art
    for a in oferta_sub_catalog:
        parent = ref_catalog[a.get("cod")]  # canonical parent from ref catalog
        key = (a.get("deviz", ""), parent, a.get("cod", ""))
        if key not in oferta_hier:
            oferta_hier[key] = a

    oferta_sub_matched: set = set()
    for ref_art in ref_subarticole:
        cod = ref_art.get("cod", "")
        parent = ref_catalog[cod]
        key = (ref_art.get("deviz", ""), parent, cod)
        deviz_cod = ref_art.get("deviz", "")
        deviz_den = ref_art.get("deviz_denumire", "")

        oferta_art = oferta_hier.get(key)
        if oferta_art is not None:
            oferta_sub_matched.add(id(oferta_art))
            diffs = compare_articles(ref_art, oferta_art, include_prices=include_prices)
            arith = check_arithmetic(oferta_art) if include_prices else []
            for d in diffs + arith:
                _enrich(d, ref_art, oferta_art, deviz_cod, deviz_den)
            neconformitati.extend(diffs + arith)
            matches.append({
                "ref_cod": cod,
                "ref_denumire": ref_art.get("denumire", ""),
                "oferta_cod": oferta_art.get("cod", ""),
                "oferta_denumire": oferta_art.get("denumire", ""),
                "deviz": deviz_cod,
            })
        else:
            nc = {"tip": "ARTICOL_LIPSA"}
            _enrich(nc, ref_art, None, deviz_cod, deviz_den)
            neconformitati.append(nc)

    # Ofertă subarticole nematchuite → ARTICOL_EXTRA
    def _make_extra_nc(oferta_art: dict) -> dict:
        deviz_den = oferta_art.get("deviz_denumire", "")
        if "e Devize" in deviz_den:
            deviz_den = deviz_den.split("e Devize")[0].strip()
        return {
            "tip": "ARTICOL_EXTRA",
            "deviz_ref": oferta_art.get("deviz", ""),
            "deviz_denumire": deviz_den,
            "is_component": True,
            "ref_cod": "", "ref_denumire": "",
            "oferta_cod": oferta_art.get("cod", ""),
            "oferta_denumire": oferta_art.get("denumire", ""),
            "oferta_um": oferta_art.get("um", ""),
            "oferta_cantitate": oferta_art.get("cantitate", ""),
            "oferta_display_parent_cod": (oferta_art.get("display_parent_cod")
                                          or ref_catalog.get(oferta_art.get("cod", ""))),
            "nr_ordine_oferta": oferta_art.get("nr_ordine"),
        }

    for a in oferta_sub_catalog:
        if id(a) not in oferta_sub_matched:
            neconformitati.append(_make_extra_nc(a))

    for a in oferta_sub_new:
        neconformitati.append(_make_extra_nc(a))

    # Caz A: $ fără parent → colectate pentru raport cu ⚠ (nu generează nc)
    caz_a_articole = ref_caz_a + oferta_sub_no_parent

    logger.info(
        f"[COMP] Layer 0: "
        f"ref_sub={len(ref_subarticole)}, "
        f"matched={len(oferta_sub_matched)}, "
        f"sub_extra={len(oferta_sub_catalog)-len(oferta_sub_matched)+len(oferta_sub_new)}, "
        f"caz_a={len(caz_a_articole)}"
    )
```

**ATENȚIE:** Mută `ref_component_cods` și `ref_by_key`/`oferta_by_key` DUPĂ blocul Layer 0 de mai sus (ele rămân pentru Layer 1-4 pe `ref_dedup`/`oferta_dedup`). Verifică că `_is_breviar_artifact` e definit o singură dată.

- [ ] **Step 2.4: Actualizează return-ul `match_global`**

```python
    return neconformitati, matches, matched_ref_keys, articole_fara_deviz + [(s, a) for a in caz_a_articole for s in ['caz_a']]
```

Sau mai explicit:

```python
    articole_speciale = articole_fara_deviz + [('caz_a', a) for a in caz_a_articole]
    return neconformitati, matches, matched_ref_keys, articole_speciale
```

- [ ] **Step 2.5: Rulează testele**

```bash
.venv/bin/python -m pytest tests/test_matching.py -v -q 2>&1 | tail -15
```
Așteptat: toate PASS.

- [ ] **Step 2.6: Verificare metrici pe date reale**

```bash
rm -f output_AO/checkpoints/*.json && .venv/bin/python local_run.py 2>&1 | grep -E "Layer 0|matched=|lipsa=|extra=|ERROR|Traceback|DONE"
```

Așteptat:
- `Layer 0:` linie prezentă în log
- `matched=` ≥ 1041 pentru OF1 (nu scade față de v7.0)
- `comparatie_oferta_1.json` conține `ARTICOL_EXTRA` pentru `$6720287`

```bash
.venv/bin/python -c "
import json; from pathlib import Path
comp = json.loads(Path('output_AO/comparatie_oferta_1.json').read_text())
extras = [n for n in comp['neconformitati'] if n.get('tip')=='ARTICOL_EXTRA' and '6720287' in str(n.get('oferta_cod',''))]
lipsa = [n for n in comp['neconformitati'] if n.get('tip')=='ARTICOL_LIPSA' and '6720289' in str(n.get('ref_cod',''))]
print('EXTRA 6720287:', extras)
print('LIPSA 6720289:', lipsa)
"
```

- [ ] **Step 2.7: Commit**

```bash
git add AgentComparator_local.py tests/test_matching.py
git commit -m "feat(comparator): Layer 0 hierarchical matching via ref_catalog — fixes false fuzzy matches for dollar subarticles"
```

---

## Task 3: Raport — Caz A (⚠) și Caz B (secțiune finală)

**Files:**
- Modify: `shared/report_builder.py`
- Modify: `shared/report_word.py`
- Modify: `local_run.py` (pasează `articole_speciale` la raport)
- Modify: `tests/test_report_builder.py`

- [ ] **Step 3.1: Scrie testele report_builder**

```python
# tests/test_report_builder.py — adaugă

def test_raport_includes_articole_fara_deviz():
    from shared.report_builder import build_raport_ierarhic
    art_fara_deviz = [
        ('ref', {'cod': 'TF24A', 'denumire': 'Fara deviz', 'deviz': '', 'nr_ordine': 1,
                 'is_component': False}),
    ]
    r = build_raport_ierarhic(REF_ARTICOLE, NECONFORMITATI, MATCHES,
                               articole_speciale=art_fara_deviz)
    assert 'articole_nelocalizate' in r
    assert any(a['cod'] == 'TF24A' for a in r['articole_nelocalizate'])

def test_raport_includes_caz_a_articole():
    from shared.report_builder import build_raport_ierarhic
    caz_a = [
        ('caz_a', {'cod': '$9999', 'denumire': 'Fara parent', 'deviz': '226108',
                   'nr_ordine': 5, 'is_component': False, 'deviz_denumire': 'Structura'}),
    ]
    r = build_raport_ierarhic(REF_ARTICOLE, NECONFORMITATI, MATCHES,
                               articole_speciale=caz_a)
    assert 'caz_a_articole' in r
    assert any(a['cod'] == '$9999' for a in r['caz_a_articole'])
```

- [ ] **Step 3.2: Rulează testele — FAIL**

```bash
.venv/bin/python -m pytest tests/test_report_builder.py::test_raport_includes_articole_fara_deviz -v
```

- [ ] **Step 3.3: Extinde `build_raport_ierarhic` în `shared/report_builder.py`**

Modifică semnătura:
```python
def build_raport_ierarhic(
    ref_articole: list,
    neconformitati: list,
    matches: list,
    articole_speciale: list = None,  # list of (tip, art) tuples
) -> dict:
```

La sfârșitul funcției, înainte de `return`:
```python
    articole_speciale = articole_speciale or []
    articole_nelocalizate = [
        {'cod': a.get('cod'), 'denumire': a.get('denumire'),
         'deviz': a.get('deviz', ''), 'nr_ordine': a.get('nr_ordine'),
         'sursa': sursa}
        for sursa, a in articole_speciale if sursa in ('ref', 'oferta')
    ]
    caz_a_articole = [
        {'cod': a.get('cod'), 'denumire': a.get('denumire'),
         'deviz': a.get('deviz'), 'deviz_denumire': a.get('deviz_denumire', ''),
         'nr_ordine': a.get('nr_ordine')}
        for sursa, a in articole_speciale if sursa == 'caz_a'
    ]

    return {
        'sumar': {...},  # existent
        'devize': devize_out,
        'erori_extractie': erori_extractie,
        'articole_nelocalizate': articole_nelocalizate,  # Caz B
        'caz_a_articole': caz_a_articole,                # Caz A
    }
```

- [ ] **Step 3.4: Actualizează `local_run.py` — pasează `articole_speciale`**

Localizare: unde se apelează `build_raport_ierarhic` (după `match_global`):

```python
    neconformitati, matches, matched_ref_keys, articole_speciale = match_global(
        ref_articles, oferta_norm, client, model, include_prices=include_prices
    )
    # ...
    raport_ierarhic = build_raport_ierarhic(
        ref_articles, neconformitati, matches,
        articole_speciale=articole_speciale
    )
    comp['raport_ierarhic'] = raport_ierarhic
```

- [ ] **Step 3.5: Actualizează `generate_word` în `shared/report_word.py`**

În `_generate_word_hierarchical`, la final (după secțiunea "Erori de extracție/OCR"), adaugă:

```python
    # Caz A: $ fără parent — afișat în deviz cu ⚠
    # (acestea sunt inserate în devizele lor în ordinea nr_ordine la Step 3.6)

    # Caz B: articole fără deviz — secțiune separată la final
    nelocalizate = raport.get('articole_nelocalizate', [])
    if nelocalizate:
        doc.add_heading("Articole nelocalizate — verificare manuală", level=2)
        doc.add_paragraph(
            "Articolele de mai jos au fost extrase dar nu au putut fi alocate "
            "unui deviz. Verificați manual în documentele PDF."
        )
        for art in nelocalizate:
            sursa = art.get('sursa', 'ref')
            doc.add_paragraph(
                f"• [{sursa.upper()}] {art.get('cod')} — {art.get('denumire')} "
                f"[nr. ordine: {art.get('nr_ordine')}]",
                style='List Bullet'
            )
```

- [ ] **Step 3.6: Caz A — rând ⚠ în deviz (în `_generate_word_hierarchical`)**

În bucla `for dv in raport.get('devize', []):`, înainte de `_add_deviz_total_row_hierarchical`, adaugă rândurile Caz A pentru devizul curent:

```python
        # Caz A: $ fără parent în devizul curent — excluse matching, afișate cu ⚠
        caz_a_in_deviz = [a for a in raport.get('caz_a_articole', [])
                          if a.get('deviz') == dv_cod]
        if caz_a_in_deviz:
            for art in caz_a_in_deviz:
                row = table.add_row().cells
                nr = art.get('nr_ordine', '')
                row[0].paragraphs[0].add_run(str(nr) if nr is not None else '')
                row[1].paragraphs[0].add_run(
                    f"{dv_cod} - {dv_den[:37]}..." if len(dv_den) > 40 else f"{dv_cod} - {dv_den}"
                ).bold = True
                cod_run = row[2].paragraphs[0].add_run(str(art.get('cod', '')))
                cod_run.bold = True
                cod_run.font.size = Pt(9)
                row[3].paragraphs[0].add_run(str(art.get('denumire', ''))[:50])
                obs_run = row[10].paragraphs[0].add_run(
                    "⚠ Articol principal neidentificat — verificare manuală (posibil eroare OCR/extracție)"
                )
                obs_run.bold = True
                obs_run.font.color.rgb = RGBColor(0xFF, 0x8C, 0x00)  # portocaliu
                for cell in row:
                    _style_cell(cell, 8)
                    _set_cell_shading(cell, "FFF3CD")  # galben deschis
```

- [ ] **Step 3.7: Rulează testele**

```bash
.venv/bin/python -m pytest tests/test_report_builder.py -v -q 2>&1 | tail -10
```

- [ ] **Step 3.8: Rulează pipeline complet și verifică DOCX**

```bash
rm -f output_AO/checkpoints/*.json && .venv/bin/python local_run.py 2>&1 | grep -E "matched=|lipsa=|extra=|ERROR|Traceback|DONE"
```

Deschide raportul și verifică:
- `$6720287` apare ca EXTRA în ofertă 1
- `$6720289` apare ca LIPSA în ofertă 1
- Layer 0 log apare în output
- Matched ≥ 1041 pentru OF1

```bash
open output_AO/Raport_Oferta_1.docx
```

- [ ] **Step 3.9: Commit**

```bash
git add shared/report_builder.py shared/report_word.py local_run.py tests/test_report_builder.py
git commit -m "feat(report): display Case A (no parent ⚠) and Case B (no deviz) articles"
```

---

## Verificare finală

```bash
# Metrici
.venv/bin/python -c "
import json; from pathlib import Path; from collections import Counter
for f in ['comparatie_oferta_1.json', 'comparatie_oferta_2.json']:
    comp = json.loads(Path(f'output_AO/{f}').read_text())
    by_tip = Counter(a.get('tip','?') for a in comp['neconformitati'])
    print(f'{f}: matched={comp[\"matches\"]}', dict(sorted(by_tip.items())))
assert True  # verify manually: matched >= 1041/1060
"

# Test specific $6720287
.venv/bin/python -c "
import json; from pathlib import Path
comp = json.loads(Path('output_AO/comparatie_oferta_1.json').read_text())
ncs = comp['neconformitati']
extra_6720287 = [n for n in ncs if '6720287' in str(n.get('oferta_cod',''))]
lipsa_6720289 = [n for n in ncs if '6720289' in str(n.get('ref_cod','')) and n.get('tip')=='ARTICOL_LIPSA']
print('EXTRA 6720287:', len(extra_6720287), extra_6720287[0].get('tip') if extra_6720287 else 'NOT FOUND')
print('LIPSA 6720289:', len(lipsa_6720289))
"

# Toate testele
.venv/bin/python -m pytest tests/ -q --ignore=tests/test_compound_deviz_extraction.py 2>&1 | tail -5
```

La succes: tag versiune.

```bash
git tag v7.1
```
