# v7.0 — Date, Matching, Raport Ierarhic — Plan Implementare

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adaugă `nr_ordine`/`parent_nr_ordine` la articole, elimină `ARTICOL_ORPHAN`, ancorează matching-ul pe referință, și generează raport DOCX ierarhic cu devize în ordinea referinței.

**Architecture:** Abordare A — secvențială. Pas 1 (date) → Pas 2 (matching) → Pas 3 (raport). Raportul DOCX existent rămâne funcțional după Pas 1 și 2 ca gardul de regresie. Pas 3 introduce un nou modul `shared/report_builder.py` care construiește structura ierarhică JSON din care `report_word.py` generează DOCX-ul restructurat.

**Tech Stack:** Python 3.11, python-docx, pytest; fără dependențe noi.

**Branch:** `feature/7.0`

**Spec:** `docs/superpowers/specs/2026-05-20-v7.0-design.md`

---

## Fișiere modificate

| Fișier | Tip | Rol |
|--------|-----|-----|
| `shared/f3_regex_parser.py` | Modify | `_make_article` + `_finalize`: adaugă `nr_ordine`, `parent_nr_ordine` |
| `shared/f3_extractor.py` | Modify | Apelează `_apply_parent_inheritance` după extracție |
| `AgentComparator_local.py` | Modify | Dedup O(n), ORPHAN eliminat, `_enrich` cu câmpuri noi |
| `local_run.py` | Modify | Elimină bloc orphan, apelează `build_raport_ierarhic` |
| `shared/report_builder.py` | Create | `build_raport_ierarhic(ref_articole, neconformitati, matches)` → JSON ierarhic |
| `shared/report_word.py` | Modify | `generate_word` redesenat: devize ierarhice în ordinea ref |
| `tests/test_nr_ordine.py` | Create | Teste pentru câmpurile noi din parser/extractor |
| `tests/test_report_builder.py` | Create | Teste pentru structura ierarhică |

---

## PAS 1 — Structura de Date

---

### Task 1: `nr_ordine` și `parent_nr_ordine` în parser

**Files:**
- Modify: `shared/f3_regex_parser.py:370-404` (`_make_article`)
- Modify: `shared/f3_regex_parser.py:793-854` (variabile locale + `_finalize`)
- Create: `tests/test_nr_ordine.py`

- [ ] **Step 1.1: Scrie testul care verifică că `nr_ordine` apare în articolele extrase**

```python
# tests/test_nr_ordine.py
import pytest
from shared.f3_regex_parser import extract_articles_regex

LINES_SIMPLE = [
    "1",
    "TF24A",
    "Turnare beton C25/30",
    "mp",
    "34.20",
    "2",
    "TF26A",
    "Turnare beton C30/37",
    "mc",
    "12.50",
]

def test_nr_ordine_main_articles():
    arts = extract_articles_regex(LINES_SIMPLE, "226108", "Structura")
    main = [a for a in arts if not a.get("is_component")]
    assert main[0].get("nr_ordine") == 1
    assert main[1].get("nr_ordine") == 2

def test_nr_ordine_none_for_subcomponent():
    lines = LINES_SIMPLE + [
        "3",
        "$3274270",
        "Cofraj lemn",
        "mp",
        "102.60",
    ]
    arts = extract_articles_regex(lines, "226108", "Structura")
    # subarticolele au nr_ordine de tip string
    subs = [a for a in arts if a.get("is_component")]
    if subs:
        assert isinstance(subs[0].get("nr_ordine"), str)

def test_parent_nr_ordine_on_subcomponent():
    lines = [
        "1", "TF24A", "Turnare beton C25/30", "mp", "34.20",
        "1.1", "$3274270", "Cofraj lemn", "mp", "102.60",
    ]
    arts = extract_articles_regex(lines, "226108", "Structura")
    subs = [a for a in arts if a.get("is_component")]
    if subs:
        assert subs[0].get("parent_nr_ordine") == 1
```

- [ ] **Step 1.2: Rulează testele — trebuie să pice (KeyError sau None)**

```bash
.venv/bin/python -m pytest tests/test_nr_ordine.py -v
```
Așteptat: FAIL — `nr_ordine` absent din dict.

- [ ] **Step 1.3: Modifică `_make_article` să accepte și stocheze `nr_ordine` și `parent_nr_ordine`**

Localizare: `shared/f3_regex_parser.py:370`

```python
def _make_article(cod: str, denumire: str, um: str, cantitate: float,
                  preturi: list, deviz_cod: str, deviz_den: str,
                  is_component: bool = False,
                  parent_code: str = None,
                  subcomponents: list = None,
                  nr_ordine=None,
                  parent_nr_ordine=None) -> Dict:
    fields = ['pret_material', 'val_material', 'pret_manopera', 'val_manopera',
              'pret_utilaj', 'val_utilaj', 'pret_transport', 'val_transport']
    art = {
        'cod': cod,
        'denumire': denumire.strip(),
        'um': um.lower(),
        'cantitate': cantitate,
        'deviz': deviz_cod,
        'deviz_denumire': deviz_den,
        'is_component': is_component,
        'parent_code': parent_code,
        'subcomponents': subcomponents or [],
        'nr_ordine': nr_ordine,
        'parent_nr_ordine': parent_nr_ordine,
    }
    for i, field in enumerate(fields):
        art[field] = preturi[i] if i < len(preturi) else 0.0
    return art
```

- [ ] **Step 1.4: Adaugă `current_parent_nr` și `sub_counter` în `extract_articles_regex`, pasează-le în `_finalize`**

Localizare: `shared/f3_regex_parser.py:793` (imediat după `last_nr_crt = 0`):

```python
last_nr_crt = 0
current_parent_nr = 0   # nr_ordine al ultimului articol principal finalizat
sub_counter = 0         # contor subarticole per articol principal curent
```

Localizare: `shared/f3_regex_parser.py:800` (în `_finalize`, extinde `nonlocal`):

```python
def _finalize():
    nonlocal cod, denumire_parts, um, cantitate, preturi, last_article_cod, \
             explicit_component_marker, current_parent_nr, sub_counter
    if cod:
        # ... (cod existent de skip-uri, neschimbat) ...

        else:
            is_subcomp_detected, parent_cod = _detect_subcomponent(cod, last_article_cod, ' '.join(denumire_parts))
            is_subcomp = explicit_component_marker or is_subcomp_detected
            subcomp_codes = _extract_subcomponent_codes(den_joined)

            if explicit_component_marker and not parent_cod:
                parent_cod = last_article_cod

            if um == 'm' and 'cub' in den_joined.lower():
                um = 'mc'

            # Calculează nr_ordine
            if not is_subcomp:
                nr_ordine = last_nr_crt if last_nr_crt else None
                parent_nr_ordine_val = None
                current_parent_nr = last_nr_crt
                sub_counter = 0
            else:
                sub_counter += 1
                nr_ordine = f"{current_parent_nr}.{sub_counter}" if current_parent_nr else None
                parent_nr_ordine_val = current_parent_nr if current_parent_nr else None

            art = _make_article(cod, den_joined, um, cantitate,
                                preturi, deviz_cod, deviz_den, is_component=is_subcomp,
                                parent_code=parent_cod,
                                subcomponents=subcomp_codes,
                                nr_ordine=nr_ordine,
                                parent_nr_ordine=parent_nr_ordine_val)
            articole.append(art)
```

**ATENȚIE:** Modificarea este în blocul `else:` final din `_finalize` (după toate skip-urile). Înlocuiești DOAR cele 3 linii `art = _make_article(...)`, `articole.append(art)`, `last_article_cod = ...` cu blocul de mai sus. Restul funcției rămâne neschimbat.

- [ ] **Step 1.5: Rulează testele**

```bash
.venv/bin/python -m pytest tests/test_nr_ordine.py -v
```
Așteptat: PASS

- [ ] **Step 1.6: Verificare rapidă pe date reale**

```bash
.venv/bin/python -c "
import json; from pathlib import Path
# Rulare pipeline pentru referinta (checkpoint existent)
from shared.f3_extractor import extract_articles_from_text_v2
txt = Path('output_AO/referinta.json').read_text()
data = json.loads(txt)
arts = data.get('articole', [])
main = [a for a in arts if not a.get('is_component')][:5]
for a in main:
    print(a.get('nr_ordine'), a.get('cod'), a.get('parent_nr_ordine'))
"
```
Așteptat: `nr_ordine` non-None pe primele articole principale.

**NOTĂ:** Dacă `output_AO/referinta.json` conține articolele deja extrase (fără re-rulare), verifica că nr_ordine e absent (cel vechi). Dacă ai nevoie de re-extracție: rulează `local_run.py` după Pas 1 complet.

- [ ] **Step 1.7: Commit**

```bash
git add shared/f3_regex_parser.py tests/test_nr_ordine.py
git commit -m "feat(parser): add nr_ordine and parent_nr_ordine to extracted articles"
```

---

### Task 2: `_apply_parent_inheritance` în extractor

**Files:**
- Modify: `shared/f3_extractor.py:663-729` (`extract_articles_from_text_v2`)
- Modify: `shared/f3_extractor.py` (adaugă funcție nouă după `inherit_component_units:130`)

- [ ] **Step 2.1: Scrie testul**

```python
# Adaugă în tests/test_nr_ordine.py

def test_apply_parent_inheritance_sets_parent_cod():
    from shared.f3_extractor import _apply_parent_inheritance
    arts = [
        {'cod': 'TF24A', 'is_component': False, 'cantitate': 34.2, 'um': 'mp', 'nr_ordine': 1},
        {'cod': '$3274270', 'is_component': True, 'cantitate': 0, 'um': '', 'nr_ordine': '1.1'},
    ]
    result = _apply_parent_inheritance(arts)
    sub = result[1]
    assert sub['parent_cod'] == 'TF24A'
    assert sub['parent_nr_ordine'] == 1
    assert sub['cant_mostenita'] is True
    assert sub['cantitate'] == 34.2
    assert sub['um'] == 'mp'
    assert sub['cantitate_originala'] == 0
    assert sub['um_originala'] == ''

def test_apply_parent_inheritance_no_override_if_has_cant():
    from shared.f3_extractor import _apply_parent_inheritance
    arts = [
        {'cod': 'TF24A', 'is_component': False, 'cantitate': 34.2, 'um': 'mp', 'nr_ordine': 1},
        {'cod': '$3274270', 'is_component': True, 'cantitate': 102.6, 'um': 'mp', 'nr_ordine': '1.1'},
    ]
    result = _apply_parent_inheritance(arts)
    sub = result[1]
    assert sub.get('cant_mostenita') is not True
    assert sub['cantitate'] == 102.6
```

- [ ] **Step 2.2: Rulează testele — trebuie să pice (ImportError)**

```bash
.venv/bin/python -m pytest tests/test_nr_ordine.py::test_apply_parent_inheritance_sets_parent_cod -v
```
Așteptat: FAIL — `ImportError: cannot import name '_apply_parent_inheritance'`

- [ ] **Step 2.3: Adaugă `_apply_parent_inheritance` în `f3_extractor.py`**

Localizare: după `inherit_component_units` (linia ~130), adaugă:

```python
def _apply_parent_inheritance(articole: list) -> list:
    """Post-procesare: subarticolele fără cant/UM moștenesc de la parinte."""
    current_parent = None
    for art in articole:
        if not art.get('is_component'):
            current_parent = art
        elif current_parent:
            art['parent_cod'] = current_parent.get('cod')
            art['parent_nr_ordine'] = current_parent.get('nr_ordine')
            if not art.get('cantitate') or not art.get('um'):
                art['cantitate_originala'] = art.get('cantitate')
                art['um_originala'] = art.get('um')
                art['cantitate'] = art.get('cantitate') or current_parent.get('cantitate')
                art['um'] = art.get('um') or current_parent.get('um')
                art['cant_mostenita'] = True
    return articole
```

- [ ] **Step 2.4: Apelează `_apply_parent_inheritance` în `extract_articles_from_text_v2`**

Localizare: `shared/f3_extractor.py:728` (după ultimul `all_articles.extend`):

```python
    logger.info(f"[F3v2] Total: {len(all_articles)} articole extrase (regex, fara LLM)")
    all_articles = _apply_parent_inheritance(all_articles)   # ← adaugă această linie
    return all_articles
```

- [ ] **Step 2.5: Rulează toate testele**

```bash
.venv/bin/python -m pytest tests/test_nr_ordine.py -v
```
Așteptat: toate PASS.

- [ ] **Step 2.6: Commit**

```bash
git add shared/f3_extractor.py tests/test_nr_ordine.py
git commit -m "feat(extractor): apply parent inheritance for subcomponent cant/UM"
```

---

## PAS 2 — Logica de Matching

> **Notă spec 2.2 — Matching ierarhic deferit la v7.1:** Spec-ul cere cheie de căutare `deviz + cod_principal + cod_secundar` pentru subarticole. Implementarea completă necesită refactorizarea `match_global` pentru context subarticol. În v7.0 matching-ul rămâne per `(deviz, cod)` — subarticolele sunt deja izolate pe deviz și codurile `$`-prefixate sunt suficient de unice în practică. Task-urile de mai jos adaugă câmpurile ierarhice și elimină ORPHAN fără a schimba algoritmul de căutare.

---

### Task 3: Deduplicare O(n) + eliminare ARTICOL_ORPHAN

**Files:**
- Modify: `AgentComparator_local.py:165-220` (`_deduplicate_neconformitati`)

- [ ] **Step 3.1: Scrie testul**

```python
# tests/test_matching.py — adaugă la fișierul existent

def test_deduplicate_on_preserves_highest_priority():
    from AgentComparator_local import _deduplicate_neconformitati
    ncs = [
        {'deviz': 'D1', 'ref_cod': 'TF24A', 'oferta_cod': 'TF24A_', 'tip': 'DIFERENTA_CAMP'},
        {'deviz': 'D1', 'ref_cod': 'TF24A', 'oferta_cod': 'TF24A_', 'tip': 'COD_SIMILAR'},
    ]
    result = _deduplicate_neconformitati(ncs)
    assert len(result) == 1
    assert result[0]['tip'] == 'COD_SIMILAR'

def test_deduplicate_no_articol_orphan_in_priority():
    from AgentComparator_local import _deduplicate_neconformitati
    # ARTICOL_ORPHAN nu mai există — nu trebuie să apară în priority map
    import inspect
    src = inspect.getsource(_deduplicate_neconformitati)
    assert 'ARTICOL_ORPHAN' not in src
```

- [ ] **Step 3.2: Rulează testele**

```bash
.venv/bin/python -m pytest tests/test_matching.py::test_deduplicate_no_articol_orphan_in_priority -v
```
Așteptat: FAIL — `ARTICOL_ORPHAN` prezent în sursă.

- [ ] **Step 3.3: Înlocuiește `_deduplicate_neconformitati` cu versiunea O(n)**

Localizare: `AgentComparator_local.py:165-220`. Înlocuiește întreaga funcție:

```python
def _deduplicate_neconformitati(neconformitati: list) -> list:
    """Remove duplicate non-conformities for the same (deviz, ref_cod, oferta_cod) pair.

    When the same article pair appears multiple times with different issue types,
    keep only the primary (most important) one. Priority order:
    1. COD_SIMILAR (code difference detected)
    2. Others (DIFERENTA_CAMP, UM_DIFERIT)
    3. ARTICOL_LIPSA, ARTICOL_EXTRA (informational)
    """
    if not neconformitati:
        return neconformitati

    priority = {
        'COD_SIMILAR': 3,
        'DIFERENTA_CAMP': 1,
        'UM_DIFERIT': 1,
        'ARTICOL_LIPSA': 0,
        'ARTICOL_EXTRA': 0,
    }

    best: dict = {}
    result_indexed: dict = {}

    for nc in neconformitati:
        key = (nc.get('deviz', ''), nc.get('ref_cod', ''), nc.get('oferta_cod', ''))
        curr_p = priority.get(nc.get('tip', ''), 0)
        if key not in best or curr_p > best[key]:
            best[key] = curr_p
            result_indexed[key] = nc

    return list(result_indexed.values())
```

- [ ] **Step 3.4: Rulează testele**

```bash
.venv/bin/python -m pytest tests/test_matching.py -v
```
Așteptat: toate PASS.

- [ ] **Step 3.5: Commit**

```bash
git add AgentComparator_local.py tests/test_matching.py
git commit -m "fix(comparator): O(n) dedup and remove ARTICOL_ORPHAN from priority"
```

---

### Task 4: Câmpuri noi în `_enrich`

**Files:**
- Modify: `AgentComparator_local.py:81-116` (`_enrich`)

- [ ] **Step 4.1: Scrie testul**

```python
# tests/test_matching.py — adaugă

def test_enrich_adds_nr_ordine_fields():
    from AgentComparator_local import _enrich
    ref_art = {
        'cod': 'TF24A', 'denumire': 'Beton', 'um': 'mp', 'cantitate': 34.2,
        'is_component': False, 'nr_ordine': 3, 'parent_cod': None,
        'parent_nr_ordine': None, 'cant_mostenita': False,
        'pret_material': 0, 'pret_manopera': 0, 'pret_utilaj': 0, 'pret_transport': 0,
        'val_material': 0, 'val_manopera': 0, 'val_utilaj': 0, 'val_transport': 0,
    }
    oferta_art = {**ref_art, 'nr_ordine': 5}
    neconf = {'tip': 'DIFERENTA_CAMP'}
    result = _enrich(neconf, ref_art, oferta_art, '226108', 'Structura')
    assert result.get('nr_ordine_ref') == 3
    assert result.get('nr_ordine_oferta') == 5
    assert result.get('parent_cod_ref') is None
    assert result.get('cant_mostenita') is False
```

- [ ] **Step 4.2: Rulează testul — trebuie să pice**

```bash
.venv/bin/python -m pytest tests/test_matching.py::test_enrich_adds_nr_ordine_fields -v
```
Așteptat: FAIL — câmpurile lipsesc din dict.

- [ ] **Step 4.3: Extinde `_enrich` cu câmpurile noi**

Localizare: `AgentComparator_local.py:84`. Adaugă în blocul `.update({...})` principal:

```python
    neconf.update({
        "deviz_ref": deviz_cod_ref,
        "deviz_denumire": deviz_den,
        "is_component": ref_art.get("is_component", False),
        "ref_cod": ref_art.get("cod", ""),
        "ref_denumire": ref_art.get("denumire", ""),
        "ref_um": ref_art.get("um", ""),
        "ref_cantitate": ref_art.get("cantitate", ""),
        "ref_pret_material": ref_art.get("pret_material", 0),
        "ref_pret_manopera": ref_art.get("pret_manopera", 0),
        "ref_pret_utilaj": ref_art.get("pret_utilaj", 0),
        "ref_pret_transport": ref_art.get("pret_transport", 0),
        "ref_val_material": ref_art.get("val_material", 0),
        "ref_val_manopera": ref_art.get("val_manopera", 0),
        "ref_val_utilaj": ref_art.get("val_utilaj", 0),
        "ref_val_transport": ref_art.get("val_transport", 0),
        # v7.0: câmpuri de context ierarhic
        "nr_ordine_ref": ref_art.get("nr_ordine"),
        "parent_cod_ref": ref_art.get("parent_cod"),
        "parent_nr_ordine_ref": ref_art.get("parent_nr_ordine"),
        "cant_mostenita": ref_art.get("cant_mostenita", False),
    })
    if oferta_art:
        neconf.update({
            "oferta_cod": oferta_art.get("cod", ""),
            "oferta_denumire": oferta_art.get("denumire", ""),
            "oferta_um": oferta_art.get("um", ""),
            "oferta_cantitate": oferta_art.get("cantitate", ""),
            "oferta_pret_material": oferta_art.get("pret_material", 0),
            "oferta_pret_manopera": oferta_art.get("pret_manopera", 0),
            "oferta_pret_utilaj": oferta_art.get("pret_utilaj", 0),
            "oferta_pret_transport": oferta_art.get("pret_transport", 0),
            "oferta_val_material": oferta_art.get("val_material", 0),
            "oferta_val_manopera": oferta_art.get("val_manopera", 0),
            "oferta_val_uitlaj": oferta_art.get("val_utilaj", 0),
            "oferta_val_transport": oferta_art.get("val_transport", 0),
            "nr_ordine_oferta": oferta_art.get("nr_ordine"),
        })
```

- [ ] **Step 4.4: Rulează testele**

```bash
.venv/bin/python -m pytest tests/test_matching.py -v
```
Așteptat: toate PASS.

- [ ] **Step 4.5: Commit**

```bash
git add AgentComparator_local.py tests/test_matching.py
git commit -m "feat(comparator): add nr_ordine and hierarchy fields to neconformitati"
```

---

### Task 5: Elimină blocul orphan din `local_run.py`

**Files:**
- Modify: `local_run.py:525` (import) și `local_run.py:645-680` (bloc orphan)

- [ ] **Step 5.1: Identifică exact liniile de șters**

```bash
grep -n "orphan\|ORPHAN\|detect_orphans" local_run.py
```

- [ ] **Step 5.2: Șterge import-ul orphan detector**

Localizare: linia cu `from shared.orphan_detector import detect_orphans` (~linia 525).
Șterge acea linie.

- [ ] **Step 5.3: Înlocuiește blocul orphan cu `orphans = []` simplu**

Blocul curent (~645-680):
```python
# Detecta orphane DUPA matching: ...
# orphans = detect_orphans(...)
orphans = []
...
for orphan in orphans:
    neconformitati.append({
        'tip': 'ARTICOL_ORPHAN',
        ...
    })
```

Înlocuiește ÎNTREGUL bloc (de la comentariul "Detecta orphane..." până la `})` de la sfârsitul for-ului) cu:

```python
# v7.0: ARTICOL_ORPHAN eliminat — articolele cu deviz greșit devin ARTICOL_EXTRA
```

- [ ] **Step 5.4: Rulează pipeline pe date de test**

```bash
.venv/bin/python local_run.py 2>&1 | tail -20
```
Așteptat: nicio eroare, metrici similare v6.f (matched count stabil, fără `ARTICOL_ORPHAN` în output).

- [ ] **Step 5.5: Verificare metrici**

```bash
.venv/bin/python -c "
import json; from pathlib import Path; from collections import Counter
for f in ['comparatie_oferta_1.json', 'comparatie_oferta_2.json']:
    comp = json.loads(Path(f'output_AO/{f}').read_text())
    by_tip = Counter(a.get('tip','?') for a in comp['neconformitati'])
    print(f'{f}: matched={comp[\"matches\"]}', dict(sorted(by_tip.items())))
    assert 'ARTICOL_ORPHAN' not in by_tip, 'ORPHAN inca prezent!'
print('OK — ARTICOL_ORPHAN absent')
"
```

- [ ] **Step 5.6: Commit**

```bash
git add local_run.py
git commit -m "feat(runner): remove ARTICOL_ORPHAN — orphan articles become ARTICOL_EXTRA"
```

---

## PAS 3 — Raportul DOCX

---

### Task 6: `build_raport_ierarhic` — modul nou

**Files:**
- Create: `shared/report_builder.py`
- Create: `tests/test_report_builder.py`

- [ ] **Step 6.1: Scrie testele**

```python
# tests/test_report_builder.py
import pytest
from shared.report_builder import build_raport_ierarhic

REF_ARTICOLE = [
    {'cod': 'TF24A', 'denumire': 'Beton C25', 'um': 'mp', 'cantitate': 34.2,
     'deviz': '226108', 'deviz_denumire': 'Structura', 'is_component': False,
     'nr_ordine': 1, 'parent_nr_ordine': None},
    {'cod': '$3274270', 'denumire': 'Cofraj', 'um': 'mp', 'cantitate': 102.6,
     'deviz': '226108', 'deviz_denumire': 'Structura', 'is_component': True,
     'nr_ordine': '1.1', 'parent_nr_ordine': 1, 'parent_cod': 'TF24A'},
    {'cod': 'TF26A', 'denumire': 'Beton C30', 'um': 'mc', 'cantitate': 12.5,
     'deviz': '226108', 'deviz_denumire': 'Structura', 'is_component': False,
     'nr_ordine': 2, 'parent_nr_ordine': None},
]

NECONFORMITATI = [
    {'tip': 'DIFERENTA_CAMP', 'deviz': '226108', 'ref_cod': 'TF26A',
     'oferta_cod': 'TF26A', 'nr_ordine_ref': 2, 'parent_cod_ref': None},
]

MATCHES = [
    {'ref_cod': 'TF24A', 'oferta_cod': 'TF24A', 'deviz': '226108'},
    {'ref_cod': '$3274270', 'oferta_cod': '$3274270', 'deviz': '226108'},
]

def test_raport_has_devize_section():
    r = build_raport_ierarhic(REF_ARTICOLE, NECONFORMITATI, MATCHES)
    assert 'devize' in r
    assert len(r['devize']) == 1
    assert r['devize'][0]['cod_deviz'] == '226108'

def test_deviz_preserves_ref_order():
    r = build_raport_ierarhic(REF_ARTICOLE, NECONFORMITATI, MATCHES)
    deviz = r['devize'][0]
    nrs = [a['nr_ordine'] for a in deviz['articole']]
    assert nrs == [1, 2]

def test_subarticole_nested_under_parent():
    r = build_raport_ierarhic(REF_ARTICOLE, NECONFORMITATI, MATCHES)
    tf24a = r['devize'][0]['articole'][0]
    assert tf24a['cod'] == 'TF24A'
    assert len(tf24a['subarticole']) == 1
    assert tf24a['subarticole'][0]['cod'] == '$3274270'

def test_status_match_correctly_set():
    r = build_raport_ierarhic(REF_ARTICOLE, NECONFORMITATI, MATCHES)
    deviz = r['devize'][0]
    tf24a = deviz['articole'][0]
    tf26a = deviz['articole'][1]
    assert tf24a['status_match'] == 'MATCHED'
    assert tf26a['status_match'] == 'NECONFORMITATE'

def test_sumar_deviz_counts():
    r = build_raport_ierarhic(REF_ARTICOLE, NECONFORMITATI, MATCHES)
    sumar = r['devize'][0]['sumar_deviz']
    assert sumar['matched'] == 1
    assert sumar['neconformitati'] == 1

def test_erori_extractie_collected():
    ref_cu_orfan = REF_ARTICOLE + [
        {'cod': '$9999', 'denumire': 'Fara parinte', 'um': 'mp', 'cantitate': 1.0,
         'deviz': '226108', 'deviz_denumire': 'Structura', 'is_component': True,
         'nr_ordine': None, 'parent_nr_ordine': None, 'parent_cod': None}
    ]
    r = build_raport_ierarhic(ref_cu_orfan, NECONFORMITATI, MATCHES)
    assert 'erori_extractie' in r
    assert any(e['cod'] == '$9999' for e in r['erori_extractie'])
```

- [ ] **Step 6.2: Rulează testele — trebuie să pice (ModuleNotFoundError)**

```bash
.venv/bin/python -m pytest tests/test_report_builder.py -v
```
Așteptat: FAIL — `ModuleNotFoundError: No module named 'shared.report_builder'`

- [ ] **Step 6.3: Implementează `shared/report_builder.py`**

```python
"""Build hierarchical report structure from flat match/nonconformity lists."""
from collections import defaultdict


def build_raport_ierarhic(
    ref_articole: list,
    neconformitati: list,
    matches: list,
) -> dict:
    """Build hierarchical report JSON from flat matching results.

    Returns dict with keys: meta, sumar, devize, erori_extractie.
    devize are in reference order; articles within each deviz are in nr_ordine order.
    """
    # Index matched ref codes per deviz
    matched_ref = set()
    for m in matches:
        matched_ref.add((m.get('deviz', ''), m.get('ref_cod', '')))

    # Index neconformitati per (deviz, ref_cod)
    nc_index = defaultdict(list)
    for nc in neconformitati:
        key = (nc.get('deviz', ''), nc.get('ref_cod', ''))
        nc_index[key].append(nc)

    # Collect subarticole fără parent (erori OCR/extracție)
    erori_extractie = []
    for art in ref_articole:
        if art.get('is_component') and not art.get('parent_cod') and not art.get('parent_nr_ordine'):
            erori_extractie.append({
                'cod': art.get('cod'),
                'denumire': art.get('denumire'),
                'deviz': art.get('deviz'),
                'deviz_denumire': art.get('deviz_denumire'),
                'nr_ordine': art.get('nr_ordine'),
            })

    # Separate main articles and subarticles
    main_arts = [a for a in ref_articole if not a.get('is_component')]
    sub_arts = [a for a in ref_articole if a.get('is_component') and a.get('parent_cod')]

    # Index subarticole per (deviz, parent_cod)
    sub_index = defaultdict(list)
    for sub in sub_arts:
        key = (sub.get('deviz', ''), sub.get('parent_cod', ''))
        sub_index[key].append(sub)

    # Build devize in reference order (preserve insertion order from ref_articole)
    deviz_order = []
    deviz_seen = set()
    for art in main_arts:
        dv = art.get('deviz', '')
        if dv not in deviz_seen:
            deviz_order.append((dv, art.get('deviz_denumire', '')))
            deviz_seen.add(dv)

    devize_out = []
    total_matched = 0
    total_lipsa = 0
    total_extra = len([nc for nc in neconformitati if nc.get('tip') == 'ARTICOL_EXTRA'])

    for dv_cod, dv_den in deviz_order:
        arts_in_deviz = [a for a in main_arts if a.get('deviz') == dv_cod]
        arts_in_deviz.sort(key=lambda a: (
            int(a['nr_ordine']) if isinstance(a.get('nr_ordine'), (int, float)) else 0
        ))

        dv_matched = 0
        dv_neconformitati = 0
        dv_lipsa = 0

        articole_out = []
        for art in arts_in_deviz:
            ref_cod = art.get('cod', '')
            ncs = nc_index.get((dv_cod, ref_cod), [])
            is_matched = (dv_cod, ref_cod) in matched_ref
            has_lipsa = any(nc.get('tip') == 'ARTICOL_LIPSA' for nc in ncs)

            if is_matched:
                status = 'MATCHED'
                dv_matched += 1
                total_matched += 1
            elif has_lipsa:
                status = 'LIPSA'
                dv_lipsa += 1
                total_lipsa += 1
            else:
                status = 'NECONFORMITATE'
                dv_neconformitati += 1

            # Subarticole ale acestui articol
            subs_ref = sub_index.get((dv_cod, ref_cod), [])
            subs_ref_sorted = sorted(subs_ref, key=lambda s: str(s.get('nr_ordine', '')))
            subarticole_out = []
            for sub in subs_ref_sorted:
                sub_cod = sub.get('cod', '')
                sub_ncs = nc_index.get((dv_cod, sub_cod), [])
                sub_matched = (dv_cod, sub_cod) in matched_ref
                sub_has_lipsa = any(nc.get('tip') == 'ARTICOL_LIPSA' for nc in sub_ncs)
                if sub_matched:
                    sub_status = 'MATCHED'
                elif sub_has_lipsa:
                    sub_status = 'LIPSA'
                else:
                    sub_status = 'NECONFORMITATE' if sub_ncs else 'MATCHED'

                subarticole_out.append({
                    'nr_ordine': sub.get('nr_ordine'),
                    'parent_cod': sub.get('parent_cod'),
                    'parent_nr_ordine': sub.get('parent_nr_ordine'),
                    'cod': sub_cod,
                    'denumire': sub.get('denumire'),
                    'um': sub.get('um'),
                    'cantitate': sub.get('cantitate'),
                    'cant_mostenita': sub.get('cant_mostenita', False),
                    'status_match': sub_status,
                    'neconformitati': sub_ncs,
                })

            articole_out.append({
                'nr_ordine': art.get('nr_ordine'),
                'cod': ref_cod,
                'denumire': art.get('denumire'),
                'um': art.get('um'),
                'cantitate': art.get('cantitate'),
                'status_match': status,
                'neconformitati': ncs,
                'subarticole': subarticole_out,
            })

        devize_out.append({
            'cod_deviz': dv_cod,
            'denumire_deviz': dv_den,
            'sumar_deviz': {
                'total': len(arts_in_deviz),
                'matched': dv_matched,
                'lipsa': dv_lipsa,
                'neconformitati': dv_neconformitati,
            },
            'articole': articole_out,
        })

    return {
        'sumar': {
            'total_articole_ref': len(main_arts),
            'matched': total_matched,
            'lipsa': total_lipsa,
            'extra': total_extra,
        },
        'devize': devize_out,
        'erori_extractie': erori_extractie,
    }
```

- [ ] **Step 6.4: Rulează testele**

```bash
.venv/bin/python -m pytest tests/test_report_builder.py -v
```
Așteptat: toate PASS.

- [ ] **Step 6.5: Commit**

```bash
git add shared/report_builder.py tests/test_report_builder.py
git commit -m "feat(report): add build_raport_ierarhic for hierarchical JSON structure"
```

---

### Task 7: `generate_word` redesenat — DOCX ierarhic

**Files:**
- Modify: `shared/report_word.py:467` (`generate_word`)
- Modify: `local_run.py` (pasează `raport_ierarhic` în `comp`)

- [ ] **Step 7.1: Adaugă `raport_ierarhic` în `comp` dict în `local_run.py`**

Localizare: în `local_run.py`, după apelul `match_global(...)` și construirea `comp` dict. Adaugă:

```python
from shared.report_builder import build_raport_ierarhic

# ... după match_global și construire neconformitati ...
raport_ierarhic = build_raport_ierarhic(ref_articole, neconformitati, matches)
comp['raport_ierarhic'] = raport_ierarhic
```

Exact după: `comp = {... "neconformitati": neconformitati, ...}` (sau imediat înainte de `generate_word`).

- [ ] **Step 7.2: Adaugă helper-e noi în `report_word.py` pentru structura ierarhică**

Adaugă aceste funcții ÎNAINTE de `generate_word` (după funcțiile existente la linia ~467):

```python
def _add_deviz_section_header(doc, dv: dict) -> None:
    """Adaugă heading deviz cu sumar matched/lipsa/neconformitati."""
    cod = dv.get('cod_deviz', '')
    den = dv.get('denumire_deviz', '')
    sumar = dv.get('sumar_deviz', {})
    heading = doc.add_heading(f"DEVIZ: {cod} — {den}", level=2)
    heading.alignment = WD_ALIGN_PARAGRAPH.LEFT
    p = doc.add_paragraph()
    p.add_run(
        f"Total: {sumar.get('total', 0)}  │  "
        f"Matched: {sumar.get('matched', 0)}  │  "
        f"Lipsă: {sumar.get('lipsa', 0)}  │  "
        f"Neconformități: {sumar.get('neconformitati', 0)}"
    ).bold = True


def _has_neconformitate(art: dict) -> bool:
    """True dacă articolul principal sau oricare subarticol are neconformitate."""
    if art.get('neconformitati') or art.get('status_match') in ('LIPSA', 'NECONFORMITATE'):
        return True
    return any(
        s.get('neconformitati') or s.get('status_match') in ('LIPSA', 'NECONFORMITATE')
        for s in art.get('subarticole', [])
    )


def _add_article_block(doc, art: dict) -> None:
    """Adaugă blocul articol principal + subarticole. Apelat doar când există neconformitate."""
    status = art.get('status_match', '')
    icon = '⚠' if status in ('LIPSA', 'NECONFORMITATE') else '✓'
    color = 'FF0000' if status in ('LIPSA', 'NECONFORMITATE') else '007700'

    p = doc.add_paragraph()
    nr = art.get('nr_ordine', '')
    run_nr = p.add_run(f"[{nr}] {art.get('cod', '')} — {art.get('denumire', '')}")
    run_nr.bold = True
    p.add_run(f"\n    Ref: {art.get('cantitate', '')} {art.get('um', '')}  │  {icon} {status}")

    for nc in art.get('neconformitati', []):
        _add_neconf_detail(doc, nc, indent=1)

    for sub in art.get('subarticole', []):
        _add_subarticol_block(doc, sub)


def _add_subarticol_block(doc, sub: dict) -> None:
    """Adaugă rând subarticol indentat cu marker ⚠ dacă are neconformitate."""
    status = sub.get('status_match', '')
    has_nc = bool(sub.get('neconformitati')) or status in ('LIPSA', 'NECONFORMITATE')
    icon = '⚠' if has_nc else '  '
    nr = sub.get('nr_ordine', '')

    p = doc.add_paragraph(style='List Bullet')
    p.paragraph_format.left_indent = Pt(24)
    label = f"[{nr}] {sub.get('cod', '')} — {sub.get('denumire', '')}"
    if has_nc:
        run = p.add_run(f"{icon} {label}")
        run.bold = True
        run.font.color.rgb = RGBColor(0xFF, 0x00, 0x00)
    else:
        p.add_run(f"   {label}")

    p.add_run(f"\n        Ref: {sub.get('cantitate', '')} {sub.get('um', '')}  │  {status}")
    if sub.get('cant_mostenita'):
        p.add_run("  [cantitate moștenită]").italic = True

    for nc in sub.get('neconformitati', []):
        _add_neconf_detail(doc, nc, indent=2)


def _add_neconf_detail(doc, nc: dict, indent: int = 1) -> None:
    """Adaugă rând detaliu neconformitate (camp diferit, valori ref vs oferta)."""
    tip = nc.get('tip', '')
    camp = nc.get('camp', '')
    val_ref = nc.get('ref_cantitate') or nc.get('ref_um') or ''
    val_oferta = nc.get('oferta_cantitate') or nc.get('oferta_um') or ''
    indent_spaces = '    ' * indent
    p = doc.add_paragraph()
    p.paragraph_format.left_indent = Pt(indent * 24)
    run = p.add_run(f"{indent_spaces}→ {tip}")
    if camp:
        run = p.add_run(f" [{camp}]")
    if val_ref or val_oferta:
        p.add_run(f": ref={val_ref}  ofertă={val_oferta}")
```

Adaugă import la topul fișierului dacă lipsește:
```python
from docx.shared import Pt, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
```

- [ ] **Step 7.3: Refactorizează `generate_word` — extrage corpul în `_generate_word_flat` și adaugă dispatch**

Localizare: `shared/report_word.py:467-646`. Pași exacți:

**a)** Redenumeste corpul existent. Adaugă o funcție nouă `_generate_word_flat` imediat ÎNAINTE de `generate_word` (linia 467), cu semnătura:

```python
def _generate_word_flat(doc, neconformitati: list, deviz_mismatches_list: list,
                         devize_extra: list, devize_lipsa: list) -> None:
```

Mută în ea tot codul din `generate_word` de la linia `neconformitati = comp.get("neconformitati", [])` (prima linie după header-ul documentului) până la sfârşitul funcției (linia 646).

**b)** Corpul `generate_word` după extragere — înlocuiește totul între header-ul documentului și `buf = io.BytesIO()` cu:

```python
    neconformitati = comp.get("neconformitati", [])
    deviz_mismatches_list = comp.get("deviz_mismatches", [])
    devize_extra = devize_extra or []
    devize_lipsa = devize_lipsa or []

    raport_ierarhic = comp.get('raport_ierarhic')
    if raport_ierarhic:
        _generate_word_hierarchical(doc, raport_ierarhic)
    else:
        _generate_word_flat(doc, neconformitati, deviz_mismatches_list,
                            devize_extra, devize_lipsa, audit_data)

    buf = io.BytesIO()
    doc.save(buf)
    return buf.getvalue()
```

Ultimele 3 linii (`buf = ...`, `doc.save(buf)`, `return buf.getvalue()`) rămân neschimbate — le muți jos după dispatch.

Adaugă funcția `_generate_word_hierarchical`:

```python
def _generate_word_hierarchical(doc, raport: dict) -> None:
    """Generează corpul documentului ierarhic: devize în ordinea ref, blocuri principal+subarticole."""
    devize = raport.get('devize', [])

    for dv in devize:
        _add_deviz_section_header(doc, dv)

        has_any_nc = any(_has_neconformitate(art) for art in dv.get('articole', []))
        if not has_any_nc:
            total = dv.get('sumar_deviz', {}).get('total', 0)
            doc.add_paragraph(f"Toate articolele conforme. Total articole principale: {total}")
            continue

        for art in dv.get('articole', []):
            if _has_neconformitate(art):
                _add_article_block(doc, art)

    # Secțiune erori extracție/OCR
    erori = raport.get('erori_extractie', [])
    if erori:
        doc.add_heading("Erori de extracție / OCR", level=2)
        p = doc.add_paragraph(
            "Subarticolele de mai jos au fost extrase fără articol principal asociat. "
            "Verificați manual în PDF-ul ofertei."
        )
        for err in erori:
            doc.add_paragraph(
                f"• {err.get('cod')} — {err.get('denumire')} "
                f"[deviz: {err.get('deviz')}]",
                style='List Bullet'
            )
```

- [ ] **Step 7.4: Rulează pipeline complet și verifică DOCX**

```bash
.venv/bin/python local_run.py 2>&1 | tail -30
```
Așteptat: nicio eroare Python; DOCX generat.

```bash
open output_AO/Raport_Oferta_1.docx
```
Verifică vizual:
- Devizele apar în ordinea din referință
- Fiecare deviz are header + sumar matched/lipsă
- Articolele cu neconformitate au bloc cu subarticole indentate
- Marcajul ⚠ roșu pe rândurile cu neconformitate
- Devize fără neconformitate → „Toate articolele conforme. Total: N"
- Secțiunea "Erori de extracție / OCR" la final (dacă există)

- [ ] **Step 7.5: Rulează suita de teste**

```bash
.venv/bin/python -m pytest tests/ -v --ignore=tests/test_compound_deviz_extraction.py
```
Așteptat: toate PASS.

- [ ] **Step 7.6: Commit final**

```bash
git add shared/report_word.py shared/report_builder.py local_run.py
git commit -m "feat(report): hierarchical DOCX with devize in reference order and nested articles"
```

---

## Verificare finală

```bash
# Metrici complete
.venv/bin/python -c "
import json; from pathlib import Path; from collections import Counter
for f in ['comparatie_oferta_1.json', 'comparatie_oferta_2.json']:
    comp = json.loads(Path(f'output_AO/{f}').read_text())
    by_tip = Counter(a.get('tip','?') for a in comp['neconformitati'])
    assert 'ARTICOL_ORPHAN' not in by_tip
    print(f'{f}: matched={comp[\"matches\"]}', dict(sorted(by_tip.items())))
print('v7.0 OK')
"

# Toate testele
.venv/bin/python -m pytest tests/ -v --ignore=tests/test_compound_deviz_extraction.py
```

La succes: tag versiune.

```bash
git tag v7.0
```
